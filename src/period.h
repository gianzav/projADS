#include <iostream>

#ifndef  PERIOD_H
#define PERIOD_H

/* Calcolo periodo frazionario in O(n^2) */
int PeriodSmart(std::string s);

/* Calcolo periodo frazionario in O(n) */
int PeriodNaive(std::string s);

#endif
